# Agents Policy Fixtures
This directory contains inert fixtures for validating the Agents workflow checks.
Fixture rules:
- Keep cases small and focused on one validator behavior.
- Use repo-relative paths only.
- Do not require live runtime state, external services, or user-specific files.
- Negative fixtures must fail for the intended reason without using placeholder
  markers or non-English prose.